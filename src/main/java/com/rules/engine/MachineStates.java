package com.rules.engine;

/**
 * <p>An enum describing the states of the combo rule parser's finite state machine, namely:</p>
 * 
 * @author Ammar Mohammed
 */
public enum MachineStates {
    FIRST, SECOND;
}